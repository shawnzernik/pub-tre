import { CSSProperties } from "react";
import { Theme } from "./Theme";

/**
 * Represents the theme for the checkbox component.
 * 
 * This object defines the styles applied to the checkbox.
 */
export const CheckboxTheme: CSSProperties = {
};
