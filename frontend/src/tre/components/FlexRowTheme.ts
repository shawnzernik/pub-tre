import { CSSProperties } from "react";
import { Theme } from "./Theme";
export const FlexRowTheme: CSSProperties = {
    width: "100%",
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    gap: "1em",
};