import * as React from "react";
import { createRoot } from "react-dom/client";

interface Props { }
interface State { }

export class Dialogue extends React.Component<Props, State> {
	public render(): React.ReactNode {
		return <div>Dialogue</div>;
	}
}