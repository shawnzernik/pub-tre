import * as React from "react";

interface Props { }
interface State { }

export class Code extends React.Component<Props, State> {
	public render(): React.ReactNode {
		return <div>Bootstrap Icon</div>;
	}
}